[
  {
    "namespace": "folderjump",
    "description": "Privileged API for injecting and managing the pinned-folder bar in the main Thunderbird window.",

    "functions": [
      {
        "name": "updateBar",
        "type": "function",
        "description": "Create or refresh the pinned-folder bar in all open 3-pane windows.",
        "async": true,
        "parameters": [
          {
            "name": "folders",
            "type": "array",
            "description": "Ordered list of pinned folders to display.",
            "items": {
              "type": "object",
              "properties": {
                "id":          { "type": "string", "description": "MailFolderId" },
                "accountId":   { "type": "string", "optional": true },
                "path":        { "type": "string", "optional": true },
                "name":        { "type": "string", "description": "Short folder name for the button label" },
                "displayPath": { "type": "string", "description": "Full path for the tooltip" }
              },
              "additionalProperties": { "type": "any" },
              "required": ["id", "name", "displayPath"]
            }
          }
        ]
      },
      {
        "name": "setUndoAvailable",
        "type": "function",
        "description": "Tell the Ctrl+Z hook whether the extension currently has a move to undo. While false the key is not intercepted, so Thunderbird's own undo runs.",
        "async": true,
        "parameters": [
          { "name": "available", "type": "boolean" }
        ]
      }
    ],

    "events": [
      {
        "name": "onFolderClicked",
        "type": "function",
        "description": "Fired when the user clicks a pinned-folder button.",
        "parameters": [
          { "name": "folderId", "type": "string" }
        ]
      },
      {
        "name": "onFolderDropped",
        "type": "function",
        "description": "Fired when messages are dropped onto a pinned-folder button.",
        "parameters": [
          { "name": "folderId", "type": "string" }
        ]
      },
      {
        "name": "onUndoRequested",
        "type": "function",
        "description": "Fired when Ctrl+Z (Cmd+Z on macOS) is pressed in a mail tab of a main window while focus is not in an editable element and setUndoAvailable(true) is in effect.",
        "parameters": []
      },
      {
        "name": "onFolderUnpinRequested",
        "type": "function",
        "description": "Fired when the user right-clicks and selects Remove from a pinned button.",
        "parameters": [
          { "name": "folderId", "type": "string" }
        ]
      }
    ]
  }
]
